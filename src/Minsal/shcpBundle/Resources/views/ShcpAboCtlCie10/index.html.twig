{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ShcpAboCtlCie10 list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Cie10nombre</th>
                <th>Cie10tipo</th>
                <th>Cie10estado</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('shcpaboctlcie10_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.cie10Nombre }}</td>
                <td>{{ entity.cie10Tipo }}</td>
                <td>{{ entity.cie10Estado }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('shcpaboctlcie10_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('shcpaboctlcie10_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('shcpaboctlcie10_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
