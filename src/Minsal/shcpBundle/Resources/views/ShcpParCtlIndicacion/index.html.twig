{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ShcpParCtlIndicacion list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Inddetalle</th>
                <th>Indestado</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('shcpparctlindicacion_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.indDetalle }}</td>
                <td>{{ entity.indEstado }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('shcpparctlindicacion_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('shcpparctlindicacion_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('shcpparctlindicacion_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
