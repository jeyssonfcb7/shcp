{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ShcpConCtlSignoAlarma list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Signombre</th>
                <th>Sigtipo</th>
                <th>Sigestado</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('shcpconctlsignoalarma_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.sigNombre }}</td>
                <td>{{ entity.sigTipo }}</td>
                <td>{{ entity.sigEstado }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('shcpconctlsignoalarma_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('shcpconctlsignoalarma_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('shcpconctlsignoalarma_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
