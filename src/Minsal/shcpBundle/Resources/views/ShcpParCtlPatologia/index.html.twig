{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ShcpParCtlPatologia list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Cpatnombrepatologia</th>
                <th>Cpattipopatologia</th>
                <th>Cpatestado</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('shcpparctlpatologia_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.cpatNombrePatologia }}</td>
                <td>{{ entity.cpatTipoPatologia }}</td>
                <td>{{ entity.cpatEstado }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('shcpparctlpatologia_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('shcpparctlpatologia_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('shcpparctlpatologia_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
