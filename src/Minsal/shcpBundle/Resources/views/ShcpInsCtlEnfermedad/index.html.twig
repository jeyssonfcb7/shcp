{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ShcpInsCtlEnfermedad list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Enfnombre</th>
                <th>Enftipo</th>
                <th>Enfestado</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('shcpinsctlenfermedad_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.enfNombre }}</td>
                <td>{{ entity.enfTipo }}</td>
                <td>{{ entity.enfEstado }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('shcpinsctlenfermedad_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('shcpinsctlenfermedad_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('shcpinsctlenfermedad_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
